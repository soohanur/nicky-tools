# Public (exposed to the browser) - must be prefixed NEXT_PUBLIC_
# Backend (FastAPI) base URL WITHOUT the /api/v1 suffix.
# Local dev: http://localhost:8000
# Production (same origin, nginx proxies /api/ to FastAPI): leave empty.
NEXT_PUBLIC_API_URL=
